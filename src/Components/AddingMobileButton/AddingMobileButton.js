import { Button } from "react-bootstrap";
import './AddingMobileButton.scss'

function AddingMobileButton() {
    return (
        <>
            <Button variant="info" className="btn-addgoal"></Button>
        </>
    );
}

export default AddingMobileButton;